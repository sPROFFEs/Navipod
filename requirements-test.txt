# Keep CI's import surface aligned with the Concierge application.  The test
# suite imports production modules during collection, so maintaining a partial
# duplicate here causes missing-module failures whenever those imports grow.
-r Navipod/concierge/requirements.txt

mutagen==1.48.1
pytest==9.1.1
pytest-cov==7.0.0
ruff==0.15.10
vulture==2.14
